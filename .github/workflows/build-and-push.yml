name: Build Docker Image

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Construir imagem
        run: cd spring && docker build -t explorando-devops:latest .      
      
  production:
    runs-on: ubuntu-latest
    needs: build
    
    steps:
      
      - name: Colocar em prod
        run: echo "Colocando em produção"